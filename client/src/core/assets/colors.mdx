import { Meta, Typeset } from "@storybook/blocks";
import { default as defaultTheme } from "tailwindcss/defaultTheme";
import { startCase } from "es-toolkit";

<Meta title="core/assets/colors" name="Colors" />

export const colors = {
	background: "--background",
	foreground: "--foreground",
	card: "--card",
	cardForeground: "--card-foreground",
	popover: "--popover",
	popoverForeground: "--popover-foreground",
	primary: "--primary",
	secondary: "--secondary",
	secondaryForeground: "--secondary-foreground",
	muted: "--muted",
	mutedForeground: "--muted-foreground",
	accent: "--accent",
	accentForeground: "--accent-foreground",
	destructive: "destructive",
	destructiveForeground: "--destructive-foreground",
	border: "--border",
	input: "--input",
	ring: "--ring",
	chart1: "--chart-1",
	chart2: "--chart-2",
	chart3: "--chart-3",
	chart4: "--chart-4",
	chart5: "--chart-5",
};

# Colors

## Light

<div className="grid grid-cols-2 md:grid-cols-4 lg:grid-cols-6 gap-8 md:gap-8 lg:gap-12 light pt-8">

{Object.entries(colors).map(([color, value]) => (

<div key={value} className="flex flex-col items-center gap-4">
	<div
		className="size-24 ring-1 ring-slate-200 ring-offset-2 ring-offset-slate-950"
		style={{ backgroundColor: `hsl(var(${value}))` }}
	/>
	<span className="text-nowrap text-sm font-bold text-white">
		{startCase(color)}
	</span>
</div>
))}

</div>

## Dark

<div className="grid grid-cols-2 md:grid-cols-4 lg:grid-cols-6 gap-8 md:gap-8 lg:gap-12 dark pt-8">

{Object.entries(colors).map(([color, value]) => (

<div key={value} className="flex flex-col items-center gap-4">
	<div
		className="size-24 ring-1 ring-slate-200 ring-offset-2 ring-offset-slate-950"
		style={{ backgroundColor: `hsl(var(${value}))` }}
	/>
	<span className="text-nowrap text-sm font-bold text-white">
		{startCase(color)}
	</span>
</div>
))}

</div>

import { describe, it } from "vitest";

describe.sequential("<DatePicker />", () => {
	it.todo("emits an input event when a new date is selected");

	it.todo("ref can be focused");

	it.todo("ref can be clicked");

	it.todo("ref can be blurred");
});

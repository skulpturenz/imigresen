import { describe, it } from "vitest";

describe("<Combobox />", () => {
	it.todo("emits an input event when new option is selected");

	it.todo("emits an input event when custom option is selected");

	it.todo("filters options based on input value");

	it.todo("filters options when a custom filter is specified");

	it.todo("creates only one custom option");

	it.todo("does not create a custom option if it already exists");

	it.todo("can be focused");

	it.todo("can be blurred");

	it.todo("can be clicked");

	it.todo("resets input value to selected option when it loses focus");

	it.todo("clears filters when it loses focus");

	it.todo("can be controlled");

	it.todo("throws an error if its content is not a render function");

	it.todo("can be cleared");
});

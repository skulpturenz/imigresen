import { describe, it } from "vitest";

describe("<Searchbox />", () => {
	it.todo("can be controlled");

	it.todo("emits an event when input value is changed");

	it.todo("allows search strings which are not valid options");

	it.todo("can be focused");

	it.todo("can be blurred");

	it.todo("can be clicked");

	it.todo("does not reset value when focus is lost");

	it.todo("throws an error if its content is not a valid JSX element");

	it.todo("can be cleared");
});

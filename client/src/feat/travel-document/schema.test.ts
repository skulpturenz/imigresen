import { describe, it } from "vitest";

describe("schema", () => {
	it.todo("optional fields when saved as draft");

	it.todo("required fields when ready to submit");

	it.todo("required fields when making an application for a dependent");
});
